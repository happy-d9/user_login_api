install the node js nd npm package
install postman and mongoDB compass
open the visual studio code and create the folder
npm i express-generator command install  in visual studio code for create folder
npm i mongoose install in visual studio code for mongooDb connection
npm i jsonwebtoken install for token generator
npm i bcrypt install for hash password 
run API : example for localhost:3000/login api run in postman 
databse include in database folder
